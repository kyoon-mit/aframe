seed_everything: 20260612

trainer:
  accelerator: auto
  devices: auto
  max_epochs: 5
  check_val_every_n_epoch: 1
  gradient_clip_val: 1.0
  log_every_n_steps: 20
  enable_progress_bar: true
  default_root_dir: /n/holystore01/LABS/iaifi_lab/Lab/kyoon/aframe/dev/runs/denoise_regress_4s/train/reg/s4d_denoise_regress_4s_test1

  logger:
    - class_path: train.callbacks.AframeWandbLogger
      init_args:
        name: s4d_denoise_regress_4s_test1
        id:   s4d_denoise_regress_4s_test1
        save_dir: /n/holystore01/LABS/iaifi_lab/Lab/kyoon/aframe/dev/runs/denoise_regress_4s/train/reg/s4d_denoise_regress_4s_test1
        project: BNS-PUBLICATION

  callbacks:
    - class_path: lightning.pytorch.callbacks.ModelCheckpoint
      init_args:
        monitor: val/mse_chirp_mass
        mode: min
        save_top_k: 3
        save_last: true
        filename: 's4d_chirp_mass_mse_{epoch:02d}'
        auto_insert_metric_name: false
    - class_path: lightning.pytorch.callbacks.ModelCheckpoint
      init_args:
        monitor: train/mse_chirp_mass
        mode: min
        save_top_k: 1
        save_last: true
        filename: 's4d_chirp_mass_mse_{epoch:02d}'
        auto_insert_metric_name: false
    - class_path: lightning.pytorch.callbacks.EarlyStopping
      init_args:
        monitor: val/mse_chirp_mass
        mode: min
        patience: 100000
    - class_path: lightning.pytorch.callbacks.LearningRateMonitor
      init_args:
        logging_interval: epoch

model:
  class_path: train.model.regression_ky.DenoisedGaussianNLLRegression
  init_args:
    arch:
      class_path: architectures.regression.RegressionTimeDomainS4DenoiseRegress
      init_args:
        num_ifos: 2
        d_output: 2          # chirp_mass mean + variance
        denoiser_d_model: 64
        denoiser_d_state: 32
        denoiser_n_layers: 4
        denoiser_dropout: 0.2
        regressor_d_model: 64
        regressor_d_state: 32
        regressor_n_layers: 4
        regressor_dropout: 0.2
        dt_min: 1.0e-3
        dt_max: 5.0
        detach_denoiser: false
    param_names:
      - chirp_mass
    beta_nll: 0.3
    y_mean: [1.80]
    y_std: [0.55]
    # test4 (lr 3e-4, restarts) destabilized after ~epoch 99; test5
    # fine-tunes test4's best ckpt gently with no restart in-window
    learning_rate: 1e-4   # non-SSM parameters
    ssm_lr: 1e-3
    normalize_input: true
    warm_start_ckpt: null
    lambda_spread: 0.2    # 0.3 spent the window-length info gain on spread; buy back mse
    lambda_denoise: 1.0
    lambda_regress: 1.0
    regress_schedule: [[0, 0.0], [2, 1.0]]  # denoiser-only 2 ep, then joint
    weight_decay: 0.01
    lr_scheduler:
      class_path: train.model.regression_ky.WarmupCosineAnnealingWarmRestarts
      init_args:
        # anneal fully within the 12h wall time so the run
        # consolidates instead of oscillating at full lr
        warmup_epochs: 1
        T_0: 140
        T_mult: 2
        eta_min: 1.0e-7
    lr_scheduler_interval: epoch

data:
  class_path: train.data.supervised.DenoisingTimeDomainSupervisedAframeDataset
  init_args:
    background_dir: /n/holystore01/LABS/iaifi_lab/Lab/kyoon/DATA/O3a_H1_L1_2048Hz
    waveforms_dir: /n/holystore01/LABS/iaifi_lab/Lab/kyoon/DATA/aframe_data/train
    ifos: [H1, L1]
    sample_rate: 2048

    kernel_length: 4.0
    model_input_sample_rate: 256
    left_pad: 3.5
    right_pad: 0.0

    batch_size: 16
    batches_per_epoch: 50
    chunk_size: 2000
    chunks_per_epoch: 25      # 25 x 2000 = 50k draws; full 200k pool every ~4 epochs
    num_files_per_batch: 1

    fduration: 1.0
    fftlength: 2.0
    psd_length: 20.0
    highpass: 20

    waveform_prob: 1.0
    swap_prob: 0.0
    mute_prob: 0.0
    max_num_workers: 8

    num_valid_views: 1
    valid_stride: 25
    valid_livetime: 210000   # 5 shifts at stride 25 -> ~8365 windows to host 8192 val waveforms

    snr_sampler:
      class_path: train.augmentations.SnrSampler
      init_args:
        constant_params:
          index: -2
          maximum: 100.0
        start_params:
          minimum: 4.0      # ckpt already snr>=4-adapted; train settled
        end_params:
          minimum: 4.0
        decay_steps: 100

    param_transforms:
      - class_path: train.transforms.ChirpMass

    waveform_sampler:
      class_path: train.data.waveforms.WaveformLoader
      init_args:
        ifos: [H1, L1]
        sample_rate: 2048
        num_val_waveforms: 8192
        val_waveform_file: /n/holystore01/LABS/iaifi_lab/Lab/kyoon/DATA/aframe_data/val/end_o3_ratesandpops_bns_snr4.hdf5
        training_waveform_path: /n/holystore01/LABS/iaifi_lab/Lab/kyoon/DATA/aframe_data/train/end_o3_ratesandpops_bns_uniform_chirp.hdf5

    dec:
      class_path: ml4gw.distributions.Cosine
    psi:
      class_path: torch.distributions.Uniform
      init_args:
        low: 0.0
        high: 3.14159
        validate_args: false
    phi:
      class_path: torch.distributions.Uniform
      init_args:
        low: -3.14159
        high: 3.14159
        validate_args: false
